# Absolute origin for this site. Drives metadataBase, sitemap.xml and robots.txt.
# Local dev falls back to localhost when unset, so this is only required in
# preview and production (set it per-environment in the Vercel dashboard).
NEXT_PUBLIC_SITE_URL="https://example.com"
